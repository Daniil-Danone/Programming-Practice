#include <iostream>

using namespace std;

int main() {
    string n;

    cin >> n;

    if (int(n[0]) + int(n[1]) + int(n[2]) == int(n[3]) + int(n[4]) + int(n[5])) {
        cout << "YES";
    } else {
        cout << "NO";
    }

    return 0;
}